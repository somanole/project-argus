# better-sqlite3 needs to run its install scripts to fetch/build the native binding
enable-pre-post-scripts=true
# keep the store inside the repo-adjacent default; deterministic hoisting for the workspace
shamefully-hoist=false
